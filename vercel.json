{
  "buildCommand": "prisma generate && prisma db push --force-reset --accept-data-loss && next build",
  "framework": "nextjs",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
